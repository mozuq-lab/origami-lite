---
description: 指定されたタスクを実行し、対象機能のテストドキュメントを機能別ディレクトリに生成します。
---

# run-task

## 目的

`/origami:split-spec` で生成されたタスク一覧から、指定されたタスクID（TASK-XXX）の機能のみを処理します。既存の4コマンド（extract-features, generate-checklist, analyze-boundaries, generate-cases）を内部的に呼び出し、**機能別ディレクトリ（`F-XXX_{機能名}/`）**に結果を出力します。

**機能別ディレクトリ方式**: 各タスクは独立した機能ディレクトリに出力し、他機能のファイルは参照しません。これによりコンテキストの肥大化を防止します。

## 事前準備

1. **追加ルールの読み込み**
   - `docs/rule` ディレクトリが存在する場合は読み込み
   - `docs/rule/origami` ディレクトリが存在する場合は読み込み
   - 各ディレクトリ内のすべてのファイルを読み込み、追加ルールとして適用

2. **タスクファイルと出力先の確認** 🔵
   - task-list.md を検索（以下の順序で検索）:
     1. `docs/origami/*/tasks/task-list.md`（仕様書別ディレクトリ）
     2. `docs/origami/tasks/task-list.md`（レガシー形式、後方互換）
   - 存在しない場合はエラーメッセージを表示し、`/origami:split-spec` の実行を案内
   - **タスクテーブルから以下の情報を取得** 🔵:
     - 機能ID（F-XXX形式）
     - 機能名（日本語論理名）
     - 出力先（`docs/origami/{仕様書名}/F-XXX_{機能名}/` 形式）

3. **仕様書ルートディレクトリの検証** 🔵
   - task-list.mdから取得した仕様書ルートディレクトリの存在を確認
   - 存在しない場合はエラーメッセージを表示し、`/origami:split-spec` の再実行を案内

## 入力

| 引数 | 説明 | 必須 | 例 |
|------|------|------|-----|
| タスクID | 実行するタスクのID | ○ | `/origami:run-task TASK-001` |

## 実行内容

**【信頼性レベル指示】**:
各項目について、入力仕様書との照合状況を以下の信号でコメントしてください：

- 🟢 **青信号**: 入力仕様書に明示的な記載がある
- 🟡 **黄信号**: テスト技法・業界標準・Web常識から推測
- 🔴 **赤信号**: 推測の根拠が薄い

### 1. 事前チェック

#### 1.1 タスク存在確認
- task-list.md から指定されたタスクIDを検索
- 該当タスクが見つからない場合はエラーメッセージを表示
- 見つかった場合は対象機能（F-XXX）を特定

#### 1.2 機能情報と出力先の取得 🔵
- task-list.mdの該当タスクセクションから以下を取得：
  - 機能ID（例: `F-001`）
  - 機能名（例: `ログイン`）
  - 出力先（例: `docs/origami/ecommerce-spec/F-001_ログイン/`）
- **出力先情報がない場合はエラー**:
  ```
  エラー: タスク {タスクID} に出力先情報が見つかりません。

  split-specコマンドを再実行してください:
  /origami:split-spec {仕様書パス}
  ```

#### 1.3 依存タスクチェック
- タスクの「依存タスク」フィールドを確認
- 依存タスクがある場合、そのチェックボックスの状態を確認
- 依存タスクが未完了の場合：
  ```
  ⚠️ 警告: 依存タスクが未完了です

  未完了の依存タスク:
  - TASK-001: {機能名} (未完了)

  続行しますか？ (y/n)
  ```
- 依存タスクがすべて完了している場合、または依存タスクがない場合は処理を続行

### 2. 機能ディレクトリの作成 🔵

**信頼性**: 🔵 *PRD「run-task変更点」より*

タスク実行開始時に、機能ディレクトリを作成します。

```
📁 機能ディレクトリを作成しています: F-XXX_{機能名}/
```

- 機能ディレクトリ（`F-XXX_{機能名}/`）が存在しない場合は作成
- 既に存在する場合はそのまま使用（上書きではなく更新）

### 3. 対象機能の処理

指定されたタスクIDに対応する機能（F-XXX）を、以下の4フェーズで順次処理します。
**各コマンドは機能ディレクトリを `--output` 引数で受け取ります** 🔵
**各フェーズは自機能フォルダ内のファイルのみを参照します（コンテキスト分離）** 🔵

#### 3.1 Phase 1: extract-features
- 対象機能の情報を `{機能ディレクトリ}/01_機能詳細.md` に出力
- コマンド: `/origami:extract-features --output {機能ディレクトリ} --target F-XXX`
- 入力: 仕様書（対象機能の該当部分のみ）
- **ファイル名**: `01_機能詳細.md`（単一機能形式）

#### 3.2 Phase 1.5: generate-checklist
- 対象機能のMust/Never動作仕様を `{機能ディレクトリ}/02_動作仕様.md` に出力
- コマンド: `/origami:generate-checklist --output {機能ディレクトリ} --target F-XXX`
- 入力: `{機能ディレクトリ}/01_機能詳細.md` のみ（他機能のファイルは参照しない）
- 信号機システムを適用
- **ファイル名**: `02_動作仕様.md`（単一機能形式）

#### 3.3 Phase 2: analyze-boundaries
- 対象機能の境界値分析表を `{機能ディレクトリ}/03_境界値分析.md` に出力
- コマンド: `/origami:analyze-boundaries --output {機能ディレクトリ} --target F-XXX`
- 入力: `{機能ディレクトリ}/02_動作仕様.md` のみ（他機能のファイルは参照しない）
- 信号機システムを適用
- **ファイル名**: `03_境界値分析.md`（単一機能形式）

#### 3.4 Phase 3: generate-cases
- 対象機能のテストケースを `{機能ディレクトリ}/04_テストケース.md` に出力
- コマンド: `/origami:generate-cases --output {機能ディレクトリ} --target F-XXX`
- 入力: `{機能ディレクトリ}/01〜03` のみ（他機能のファイルは参照しない）
- TC-XXX-YY 形式でテストケースIDを付与
- 信号機システムを適用
- **ファイル名**: `04_テストケース.md`（単一機能形式）

### 4. タスク完了処理

- task-list.md の該当タスクのチェックボックスを更新
  ```markdown
  - [x] TASK-001 完了
  ```
- タスクの「状態」を「完了」に更新

## 出力フォーマット

### 実行開始時

```
🚀 タスク実行開始: {タスクID}

📋 タスク詳細:
- タスクID: {タスクID}
- 機能ID: F-XXX
- 機能名: {機能名}
- 出力先: docs/origami/{仕様書名}/F-XXX_{機能名}/
- 依存タスク: {あり/なし}

📁 機能ディレクトリを作成しました: F-XXX_{機能名}/

🔄 処理フェーズを開始します...
```

### 各フェーズ完了時

```
✅ Phase 1 完了: extract-features
   → F-XXX_{機能名}/01_機能詳細.md を作成しました

✅ Phase 1.5 完了: generate-checklist
   → F-XXX_{機能名}/02_動作仕様.md を作成しました
   → 入力: 01_機能詳細.md のみ（コンテキスト分離）
   → 🟢: X件 / 🟡: X件 / 🔴: X件

✅ Phase 2 完了: analyze-boundaries
   → F-XXX_{機能名}/03_境界値分析.md を作成しました
   → 入力: 02_動作仕様.md のみ（コンテキスト分離）
   → 🟢: X件 / 🟡: X件 / 🔴: X件

✅ Phase 3 完了: generate-cases
   → F-XXX_{機能名}/04_テストケース.md を作成しました
   → 入力: 01〜03 のみ（コンテキスト分離）
   → テストケース数: X件
   → 🟢: X件 / 🟡: X件 / 🔴: X件
```

### タスク完了時

```
🎉 タスク完了: {タスクID}

📊 実行サマリー:
- 機能ID: F-XXX
- 機能名: {機能名}
- 出力先: docs/origami/{仕様書名}/F-XXX_{機能名}/
- 生成テストケース数: X件
- 信号分布: 🟢 X件 / 🟡 X件 / 🔴 X件

📁 作成されたファイル:
- docs/origami/{仕様書名}/F-XXX_{機能名}/01_機能詳細.md
- docs/origami/{仕様書名}/F-XXX_{機能名}/02_動作仕様.md
- docs/origami/{仕様書名}/F-XXX_{機能名}/03_境界値分析.md
- docs/origami/{仕様書名}/F-XXX_{機能名}/04_テストケース.md

📁 更新されたファイル:
- docs/origami/{仕様書名}/tasks/task-list.md

🔜 次のステップ:
- 次のタスクを実行: /origami:run-task {次のタスクID}
- 進捗確認: /origami:verify-tasks {仕様書名}
```

## エラーハンドリング

| エラーケース | メッセージ |
|------------|----------|
| タスクファイル未存在 | 「タスク一覧ファイルが見つかりません。先に `/origami:split-spec` を実行してください」 |
| 出力先情報なし | 「タスク {タスクID} に出力先情報が見つかりません。`/origami:split-spec` を再実行してください」 |
| 仕様書ディレクトリ未存在 | 「仕様書ディレクトリが存在しません: {パス}。`/origami:split-spec` を再実行してください」 |
| タスクID未指定 | 「タスクIDを指定してください。例: `/origami:run-task TASK-001`」 |
| 不正なタスクID形式 | 「タスクIDの形式が不正です。TASK-XXX 形式で指定してください」 |
| タスクが見つからない | 「タスク {タスクID} が見つかりません。有効なタスクID: TASK-001, TASK-002, ...」 |
| 依存タスク未完了（中断時） | 「依存タスクの完了後に再実行してください」 |
| タスク既に完了 | 「タスク {タスクID} は既に完了しています。再実行しますか？ (y/n)」 |
| 機能ディレクトリ作成失敗 | 「機能ディレクトリを作成できません: {パス}」 |

## 機能情報の解析方法 🔵

task-list.md の各タスクセクションから機能情報と出力先を取得する方法:

```markdown
### TASK-001: F-001 ログイン

| 項目 | 内容 |
|------|------|
| タスクID | TASK-001 |
| 機能ID | F-001 |                                          ← 機能ID
| 機能名 | ログイン |                                      ← 機能名
| 出力先 | docs/origami/ecommerce-spec/F-001_ログイン/ | ← 出力先
| 処理フェーズ | extract-features → generate-checklist → analyze-boundaries → generate-cases |
| 依存タスク | なし |
| 状態 | 未完了 |
```

**解析ルール**:
1. 指定されたタスクIDのセクションを検索
2. テーブルから「機能ID」「機能名」「出力先」行を解析
3. 各値のコロン以降の文字列を取得
4. 前後の空白を除去
5. 出力先パスの末尾に `/` がない場合は追加

## 実行後の確認

- 機能ディレクトリ（`F-XXX_{機能名}/`）が作成されたことを確認
- 各出力ファイル（`01_機能詳細.md`〜`04_テストケース.md`）が作成されたことを確認
- task-list.md のチェックボックスが更新されたことを確認
- 信号分布のサマリーを表示し、🔴項目がある場合は警告

## 関連コマンド

| コマンド | 説明 |
|---------|------|
| `/origami:split-spec` | タスク分割計画を生成（run-taskの前に実行、機能情報と出力先を生成） |
| `/origami:verify-tasks` | 機能ディレクトリ構造とタスク完了状況を確認 |
| `/origami:extract-features` | 機能詳細を抽出（run-task内で呼び出される、`--output`で機能ディレクトリを指定） |
| `/origami:generate-checklist` | 動作仕様を作成（run-task内で呼び出される、入力は同一機能フォルダのみ） |
| `/origami:analyze-boundaries` | 境界値分析を作成（run-task内で呼び出される、入力は同一機能フォルダのみ） |
| `/origami:generate-cases` | テストケースを生成（run-task内で呼び出される、入力は同一機能フォルダのみ） |

## 機能別ディレクトリ方式について

### ディレクトリ構造

run-task実行後のディレクトリ構造:

```text
docs/origami/{仕様書名}/
├── F-001_ログイン/
│   ├── 01_機能詳細.md
│   ├── 02_動作仕様.md
│   ├── 03_境界値分析.md
│   └── 04_テストケース.md
├── F-002_ユーザー登録/
│   └── ...（別タスクで生成）
└── tasks/
    └── task-list.md
```

### コンテキスト分離

各フェーズは自機能フォルダ内のファイルのみを参照します：

| フェーズ | 入力 | 出力 |
|---------|------|------|
| Phase 1 | 仕様書（対象機能部分のみ） | `01_機能詳細.md` |
| Phase 1.5 | `01_機能詳細.md` のみ | `02_動作仕様.md` |
| Phase 2 | `02_動作仕様.md` のみ | `03_境界値分析.md` |
| Phase 3 | `01〜03` のみ | `04_テストケース.md` |

**重要**: 他機能ディレクトリ（`F-002_ユーザー登録/`等）のファイルは参照しません。

### メリット

1. **コンテキスト一定**: 各タスク実行時、自機能フォルダのみ参照
2. **独立性**: 機能単位で完結、並列レビュー可能
3. **再実行容易**: 特定機能だけ再生成可能
4. **可読性向上**: 機能論理名でディレクトリ内容が明確

### Breaking Change（v3.0.0）

- 既存の追記方式（`01_機能一覧.md` への追記）との互換性はありません
- 単独実行時のデフォルト動作は変更なし（後方互換）
